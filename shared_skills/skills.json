{"mineWoodLog": {"code": "async function mineWoodLog(bot) {\n  const mcData = require('minecraft-data')(bot.version);\n\n  // Step 1: Explore until we find a wood log (acacia_log in savanna)\n  const logBlock = await exploreUntil(bot, new Vec3(Math.random(), 0, Math.random()), 60, () => {\n    return bot.findBlock({\n      matching: mcData.blocksByName[\"acacia_log\"].id,\n      maxDistance: 32\n    });\n  });\n  if (logBlock) {\n    bot.chat(\"Found an acacia log, mining it now.\");\n    // Step 2: Mine the wood log\n    await mineBlock(bot, \"acacia_log\", 1);\n    bot.chat(\"Mined 1 acacia log.\");\n  } else {\n    bot.chat(\"Could not find any acacia logs nearby.\");\n  }\n}", "description": "async function mineWoodLog(bot) {\n    // The function focuses on mining an acacia log. It begins by exploring the surroundings to locate a wood log within a specified distance. If an acacia log is found, it proceeds to mine the log. After successfully mining the log, a message is sent to indicate the action was completed. If no acacia logs are found, a message is sent to inform that none are nearby.\n}", "created_by": "scout", "created_by_name": "Scout", "created_at": "2025-12-28T18:11:53.071505", "version": 3, "success_count": 0, "fail_count": 0}, "craftWoodenPickaxe": {"code": "async function craftWoodenPickaxe(bot) {\n  const mcData = require('minecraft-data')(bot.version);\n\n  // Step 1: Place the crafting table near the bot\n  const craftingTablePosition = bot.entity.position.offset(1, 0, 0);\n  await placeItem(bot, \"crafting_table\", craftingTablePosition);\n  bot.chat(\"Placed the crafting table.\");\n\n  // Step 2: Craft the wooden pickaxe\n  await craftItem(bot, \"wooden_pickaxe\", 1);\n  bot.chat(\"Crafted a wooden pickaxe.\");\n}", "description": "async function craftWoodenPickaxe(bot) {\n    // The function focuses on crafting a wooden pickaxe. It begins by placing a crafting table near the bot's current position. After the crafting table is set up, it proceeds to craft one wooden pickaxe. The function ensures that the necessary steps for crafting are completed successfully. Finally, it confirms the successful crafting of the wooden pickaxe.\n}", "created_by": "legacy", "created_by_name": "Legacy Bot", "created_at": "2025-12-28T17:22:11.031725", "version": 1, "success_count": 0, "fail_count": 0, "migrated_from": "ckpt/skill"}, "mineIronOre": {"code": "async function mineIronOre(bot) {\n  const mcData = require('minecraft-data')(bot.version);\n\n  // Step 1: Locate the iron ore block nearby\n  const ironOreBlock = bot.findBlock({\n    matching: mcData.blocksByName[\"iron_ore\"].id,\n    maxDistance: 32\n  });\n  if (ironOreBlock) {\n    bot.chat(\"Found iron ore, mining it now.\");\n\n    // Step 2: Use the stone pickaxe to mine the iron ore\n    await mineBlock(bot, \"iron_ore\", 1);\n    bot.chat(\"Mined 1 iron ore.\");\n  } else {\n    bot.chat(\"Could not find any iron ore nearby.\");\n  }\n}", "description": "async function mineIronOre(bot) {\n    // The function focuses on mining a single iron ore block. It first searches for an iron ore block within a 32-block radius. If an iron ore block is found, it proceeds to mine it using a stone pickaxe. After successfully mining the iron ore, a message is sent to indicate the action was completed. If no iron ore is found, a message is sent stating that no iron ore is nearby.\n}", "created_by": "legacy", "created_by_name": "Legacy Bot", "created_at": "2025-12-28T17:22:11.032289", "version": 1, "success_count": 0, "fail_count": 0, "migrated_from": "ckpt/skill"}, "mineCoalOre": {"code": "async function mineCoalOre(bot) {\n  const mcData = require('minecraft-data')(bot.version);\n\n  // Step 1: Locate the nearest coal ore block\n  const coalOreBlock = bot.findBlock({\n    matching: mcData.blocksByName[\"coal_ore\"].id,\n    maxDistance: 32\n  });\n  if (coalOreBlock) {\n    bot.chat(\"Found coal ore, mining it now.\");\n\n    // Step 2: Mine the coal ore block\n    await mineBlock(bot, \"coal_ore\", 1);\n    bot.chat(\"Mined 1 coal ore.\");\n  } else {\n    bot.chat(\"Could not find any coal ore nearby.\");\n  }\n}", "description": "async function mineCoalOre(bot) {\n    // The function focuses on mining a coal ore block. It first searches for the nearest coal ore within a 32-block radius. If a coal ore block is found, it proceeds to mine that block. After successfully mining the coal ore, it confirms the action. If no coal ore is located, it indicates that there are none nearby.\n}", "created_by": "legacy", "created_by_name": "Legacy Bot", "created_at": "2025-12-28T17:22:11.033499", "version": 1, "success_count": 0, "fail_count": 0, "migrated_from": "ckpt/skill"}, "mineThreeCoalOre": {"code": "async function mineThreeCoalOre(bot) {\n  const mcData = require('minecraft-data')(bot.version);\n\n  // Step 1: Locate the nearest coal ore blocks\n  let coalOreBlocks = bot.findBlocks({\n    matching: mcData.blocksByName[\"coal_ore\"].id,\n    maxDistance: 32,\n    count: 3\n  });\n\n  // Step 2: Check if there are enough coal ore blocks\n  if (coalOreBlocks.length >= 3) {\n    bot.chat(\"Found enough coal ore, preparing to mine.\");\n\n    // Step 3: Equip the iron pickaxe if not already equipped\n    const ironPickaxe = bot.inventory.findInventoryItem(mcData.itemsByName[\"iron_pickaxe\"].id);\n    if (ironPickaxe) {\n      await bot.equip(ironPickaxe, \"hand\");\n      bot.chat(\"Equipped iron pickaxe.\");\n    } else {\n      bot.chat(\"No iron pickaxe available.\");\n      return;\n    }\n\n    // Step 4: Mine the coal ore blocks\n    await mineBlock(bot, \"coal_ore\", 3);\n    bot.chat(\"Mined 3 coal ore.\");\n  } else {\n    bot.chat(\"Could not find enough coal ore nearby.\");\n  }\n}", "description": "async function mineThreeCoalOre(bot) {\n    // The function focuses on mining three coal ore blocks. It begins by locating the nearest coal ore blocks within a specified distance. If there are at least three coal ore blocks available, it proceeds to check for an iron pickaxe in the inventory. If an iron pickaxe is found, it equips the pickaxe and then mines the three coal ore blocks. If there are not enough coal ore blocks or no iron pickaxe is available, appropriate messages are displayed.\n}", "created_by": "bot", "created_by_name": "bot", "created_at": "2025-12-28T17:58:04.305201", "version": 2, "success_count": 0, "fail_count": 0}, "mineOneCoalOre": {"code": "async function mineOneCoalOre(bot) {\n  const mcData = require('minecraft-data')(bot.version);\n\n  // Step 1: Explore until a coal ore block is found\n  const coalOreBlock = await exploreUntil(bot, new Vec3(Math.random(), 0, Math.random()), 60, () => {\n    return bot.findBlock({\n      matching: mcData.blocksByName[\"coal_ore\"].id,\n      maxDistance: 32\n    });\n  });\n\n  // Step 2: Check if a coal ore block was found\n  if (coalOreBlock) {\n    bot.chat(\"Found coal ore, mining it now.\");\n\n    // Step 3: Equip the iron pickaxe if not already equipped\n    const ironPickaxe = bot.inventory.findInventoryItem(mcData.itemsByName[\"iron_pickaxe\"].id);\n    if (ironPickaxe) {\n      await bot.equip(ironPickaxe, \"hand\");\n    }\n\n    // Step 4: Mine the coal ore block\n    await mineBlock(bot, \"coal_ore\", 1);\n    bot.chat(\"Mined 1 coal ore.\");\n  } else {\n    bot.chat(\"Could not find any coal ore nearby.\");\n  }\n}", "description": "async function mineOneCoalOre(bot) {\n    // The function focuses on mining a single coal ore block. It begins by exploring the area until a coal ore block is located. If a coal ore block is found, it proceeds to check for an iron pickaxe in the inventory and equips it if available. After equipping the iron pickaxe, the function mines the coal ore block. If no coal ore is found, it indicates that there are no nearby coal ore blocks.\n}", "created_by": "legacy", "created_by_name": "Legacy Bot", "created_at": "2025-12-28T17:22:11.036611", "version": 1, "success_count": 0, "fail_count": 0, "migrated_from": "ckpt/skill"}, "checkItemsInChest": {"code": "async function checkItemsInChest(bot) {\n  const chestPosition = new Vec3(-1022, 63, -193);\n  await checkItemInsideChest(bot, chestPosition);\n}", "description": "async function checkItemsInChest(bot) {\n    // The function checks for items inside a specific chest located at the coordinates (-1022, 63, -193). It utilizes a helper function to perform the check on the chest's contents. The bot moves to the designated chest position to access it. The primary goal is to determine what items are available in that chest. This function is essential for inventory management and resource gathering.\n}", "created_by": "legacy", "created_by_name": "Legacy Bot", "created_at": "2025-12-28T17:22:11.037744", "version": 1, "success_count": 0, "fail_count": 0, "migrated_from": "ckpt/skill"}, "checkItemsInSpecificChest": {"code": "async function checkItemsInSpecificChest(bot) {\n  const chestPosition = new Vec3(-1007, 63, -195);\n  await moveToChest(bot, chestPosition);\n  const chestBlock = bot.blockAt(chestPosition);\n  const chest = await bot.openContainer(chestBlock);\n  bot.chat(\"Opened the chest at (-1007, 63, -195). Checking items...\");\n  // Assuming you want to log the items in the chest\n  const items = chest.containerItems();\n  items.forEach(item => {\n    bot.chat(`Item: ${item.name}, Count: ${item.count}`);\n  });\n  await closeChest(bot, chestBlock);\n  bot.chat(\"Checked items and closed the chest.\");\n}", "description": "async function checkItemsInSpecificChest(bot) {\n    // The function involves checking the items in a specific chest located at the coordinates (-1007, 63, -195). It first moves the bot to the chest's position and opens the chest. After opening the chest, it logs the names and counts of the items contained within. Once the items have been checked, the function closes the chest. Finally, it confirms that the items have been checked and the chest has been closed.\n}", "created_by": "legacy", "created_by_name": "Legacy Bot", "created_at": "2025-12-28T17:22:11.038662", "version": 1, "success_count": 0, "fail_count": 0, "migrated_from": "ckpt/skill"}, "mineCopperOre": {"code": "async function mineCopperOre(bot) {\n  const mcData = require('minecraft-data')(bot.version);\n\n  // Step 1: Locate the copper ore block nearby\n  const copperOreBlock = bot.findBlock({\n    matching: mcData.blocksByName[\"copper_ore\"].id,\n    maxDistance: 32\n  });\n  if (copperOreBlock) {\n    bot.chat(\"Found copper ore, preparing to mine it.\");\n\n    // Step 2: Equip a stone pickaxe\n    const stonePickaxe = bot.inventory.findInventoryItem(mcData.itemsByName[\"stone_pickaxe\"].id);\n    if (stonePickaxe) {\n      await bot.equip(stonePickaxe, \"hand\");\n      bot.chat(\"Equipped stone pickaxe.\");\n\n      // Step 3: Mine the copper ore block\n      await mineBlock(bot, \"copper_ore\", 1);\n      bot.chat(\"Mined 1 copper ore.\");\n    } else {\n      bot.chat(\"No stone pickaxe available.\");\n    }\n  } else {\n    bot.chat(\"Could not find any copper ore nearby.\");\n  }\n}", "description": "async function mineCopperOre(bot) {\n    // The function focuses on mining a copper ore block if one is found nearby. It first searches for a copper ore block within a 32-block radius. If a copper ore block is located, it checks for the availability of a stone pickaxe in the inventory. If the stone pickaxe is available, it equips the pickaxe and proceeds to mine the copper ore block. After successfully mining the copper ore, it confirms the action. If no copper ore or stone pickaxe is found, appropriate messages are generated.\n}", "created_by": "legacy", "created_by_name": "Legacy Bot", "created_at": "2025-12-28T17:22:11.039653", "version": 1, "success_count": 0, "fail_count": 0, "migrated_from": "ckpt/skill"}, "mineOneIronOre": {"code": "async function mineOneIronOre(bot) {\n  const mcData = require('minecraft-data')(bot.version);\n\n  // Step 1: Explore until an iron ore block is found\n  const ironOreBlock = await exploreUntil(bot, new Vec3(0, -1, 0), 60, () => {\n    return bot.findBlock({\n      matching: mcData.blocksByName[\"iron_ore\"].id,\n      maxDistance: 32\n    });\n  });\n\n  // Step 2: Check if an iron ore block was found\n  if (ironOreBlock) {\n    bot.chat(\"Found iron ore, preparing to mine it.\");\n\n    // Step 3: Equip the iron pickaxe if not already equipped\n    const ironPickaxe = bot.inventory.findInventoryItem(mcData.itemsByName[\"iron_pickaxe\"].id);\n    if (ironPickaxe) {\n      await bot.equip(ironPickaxe, \"hand\");\n      bot.chat(\"Equipped iron pickaxe.\");\n    } else {\n      bot.chat(\"No iron pickaxe available.\");\n      return;\n    }\n\n    // Step 4: Mine the iron ore block\n    await mineBlock(bot, \"iron_ore\", 1);\n    bot.chat(\"Mined 1 iron ore.\");\n  } else {\n    bot.chat(\"Could not find any iron ore nearby.\");\n  }\n}", "description": "async function mineOneIronOre(bot) {\n    // The function focuses on mining a single iron ore block. It begins by exploring the surroundings until an iron ore block is located. If an iron ore block is found, it checks for the availability of an iron pickaxe in the inventory. If the iron pickaxe is available, it equips it and proceeds to mine the iron ore block. If no iron ore is found or the pickaxe is unavailable, appropriate messages are communicated.\n}", "created_by": "bot", "created_by_name": "bot", "created_at": "2025-12-28T17:53:18.182483", "version": 2, "success_count": 0, "fail_count": 0}, "mineOneCopperOre": {"code": "async function mineOneCopperOre(bot) {\n  const mcData = require('minecraft-data')(bot.version);\n\n  // Step 1: Locate the nearest copper ore block\n  const copperOreBlock = bot.findBlock({\n    matching: mcData.blocksByName[\"copper_ore\"].id,\n    maxDistance: 32\n  });\n\n  // Step 2: Check if a copper ore block was found\n  if (copperOreBlock) {\n    bot.chat(\"Found copper ore, preparing to mine it.\");\n\n    // Step 3: Equip the iron pickaxe if not already equipped\n    const ironPickaxe = bot.inventory.findInventoryItem(mcData.itemsByName[\"iron_pickaxe\"].id);\n    if (ironPickaxe) {\n      await bot.equip(ironPickaxe, \"hand\");\n      bot.chat(\"Equipped iron pickaxe.\");\n    } else {\n      bot.chat(\"No iron pickaxe available.\");\n      return;\n    }\n\n    // Step 4: Mine the copper ore block\n    await mineBlock(bot, \"copper_ore\", 1);\n    bot.chat(\"Mined 1 copper ore.\");\n  } else {\n    bot.chat(\"Could not find any copper ore nearby.\");\n  }\n}", "description": "async function mineOneCopperOre(bot) {\n    // The function focuses on mining a single copper ore block. It begins by locating the nearest copper ore within a specified distance. If a copper ore block is found, it checks for an iron pickaxe in the inventory and equips it if available. Once equipped, the function proceeds to mine the copper ore block. If no copper ore is found or the iron pickaxe is unavailable, appropriate messages are displayed.\n}", "created_by": "bot", "created_by_name": "bot", "created_at": "2025-12-28T17:46:29.465027", "version": 2, "success_count": 0, "fail_count": 0}, "smeltRawCopper": {"code": "async function smeltRawCopper(bot) {\n  const mcData = require('minecraft-data')(bot.version);\n\n  // Step 1: Check if a furnace is already placed nearby\n  let furnaceBlock = bot.findBlock({\n    matching: mcData.blocksByName.furnace.id,\n    maxDistance: 32\n  });\n\n  // If no furnace is found, place one from the inventory\n  if (!furnaceBlock) {\n    const furnacePosition = bot.entity.position.offset(1, 0, 0);\n    await placeItem(bot, \"furnace\", furnacePosition);\n    bot.chat(\"Placed a furnace.\");\n    furnaceBlock = bot.blockAt(furnacePosition);\n  }\n\n  // Step 2: Smelt 8 raw copper using coal as fuel\n  const rawCopperCount = 8;\n  const fuelName = \"coal\";\n  await smeltItem(bot, \"raw_copper\", fuelName, rawCopperCount);\n  bot.chat(\"Smelted 8 raw copper into copper ingots.\");\n}", "description": "async function smeltRawCopper(bot) {\n    // The function focuses on smelting 8 raw copper items into copper ingots. It first checks for the presence of a furnace within a 32-block radius. If no furnace is found, it places one from the inventory at a specified position. After ensuring a furnace is available, it proceeds to smelt the raw copper using coal as fuel. Finally, it confirms the successful smelting of the copper ingots.\n}", "created_by": "legacy", "created_by_name": "Legacy Bot", "created_at": "2025-12-28T17:22:11.043513", "version": 1, "success_count": 0, "fail_count": 0, "migrated_from": "ckpt/skill"}, "mineFiveStoneBlocks": {"code": "async function mineFiveStoneBlocks(bot) {\n  const mcData = require('minecraft-data')(bot.version);\n\n  // Step 1: Locate the nearest stone blocks\n  let stoneBlocks = bot.findBlocks({\n    matching: mcData.blocksByName[\"stone\"].id,\n    maxDistance: 32,\n    count: 5\n  });\n\n  // Step 2: If not enough stone blocks are found, explore until we find them\n  if (stoneBlocks.length < 5) {\n    bot.chat(\"Not enough stone nearby, exploring to find more.\");\n    stoneBlocks = await exploreUntil(bot, new Vec3(Math.random(), 0, Math.random()), 60, () => {\n      return bot.findBlocks({\n        matching: mcData.blocksByName[\"stone\"].id,\n        maxDistance: 32,\n        count: 5\n      });\n    });\n  }\n\n  // Step 3: Mine the stone blocks\n  if (stoneBlocks.length >= 5) {\n    bot.chat(\"Found enough stone, mining now.\");\n    await mineBlock(bot, \"stone\", 5);\n    bot.chat(\"Mined 5 stone blocks.\");\n  } else {\n    bot.chat(\"Could not find enough stone.\");\n  }\n}", "description": "async function mineFiveStoneBlocks(bot) {\n    // The function focuses on mining five stone blocks. It first attempts to locate nearby stone blocks within a specified distance. If fewer than five stone blocks are found, it explores the area until enough stone blocks are located. Once five or more stone blocks are identified, it proceeds to mine them. If the required number of stone blocks cannot be found, it indicates the failure to locate enough stone.\n}", "created_by": "bot", "created_by_name": "bot", "created_at": "2025-12-28T17:44:07.610779", "version": 1, "success_count": 0, "fail_count": 0}, "mineFiveCoalOre": {"code": "async function mineFiveCoalOre(bot) {\n  const mcData = require('minecraft-data')(bot.version);\n\n  // Step 1: Locate the nearest coal ore blocks\n  let coalOreBlocks = bot.findBlocks({\n    matching: mcData.blocksByName[\"coal_ore\"].id,\n    maxDistance: 32,\n    count: 5\n  });\n\n  // Step 2: If not enough coal ore blocks are found, explore until we find them\n  if (coalOreBlocks.length < 5) {\n    bot.chat(\"Not enough coal ore nearby, exploring to find more.\");\n    coalOreBlocks = await exploreUntil(bot, new Vec3(Math.random(), 0, Math.random()), 60, () => {\n      return bot.findBlocks({\n        matching: mcData.blocksByName[\"coal_ore\"].id,\n        maxDistance: 32,\n        count: 5\n      });\n    });\n  }\n\n  // Step 3: Mine the coal ore blocks\n  if (coalOreBlocks.length >= 5) {\n    bot.chat(\"Found enough coal ore, mining now.\");\n    await mineBlock(bot, \"coal_ore\", 5);\n    bot.chat(\"Mined 5 coal ore.\");\n  } else {\n    bot.chat(\"Could not find enough coal ore.\");\n  }\n}", "description": "async function mineFiveCoalOre(bot) {\n    // The function focuses on mining five coal ore blocks. It first attempts to locate nearby coal ore blocks within a specified distance. If fewer than five blocks are found, it explores the area until enough coal ore is located. Once sufficient coal ore blocks are identified, the function proceeds to mine them. If the required number of coal ore blocks cannot be found, it indicates the failure to locate enough resources.\n}", "created_by": "bot", "created_by_name": "bot", "created_at": "2025-12-28T17:49:16.245755", "version": 2, "success_count": 0, "fail_count": 0}, "mineLapisOre": {"code": "async function mineLapisOre(bot) {\n  const mcData = require('minecraft-data')(bot.version);\n\n  // Step 1: Locate the lapis ore block nearby\n  const lapisOreBlock = bot.findBlock({\n    matching: mcData.blocksByName[\"lapis_ore\"].id,\n    maxDistance: 32\n  });\n\n  // Step 2: Check if a lapis ore block was found\n  if (lapisOreBlock) {\n    bot.chat(\"Found lapis ore, preparing to mine it.\");\n\n    // Step 3: Equip the iron pickaxe if not already equipped\n    const ironPickaxe = bot.inventory.findInventoryItem(mcData.itemsByName[\"iron_pickaxe\"].id);\n    if (ironPickaxe) {\n      await bot.equip(ironPickaxe, \"hand\");\n      bot.chat(\"Equipped iron pickaxe.\");\n    } else {\n      bot.chat(\"No iron pickaxe available.\");\n      return;\n    }\n\n    // Step 4: Mine the lapis ore block\n    await mineBlock(bot, \"lapis_ore\", 1);\n    bot.chat(\"Mined 1 lapis ore.\");\n  } else {\n    bot.chat(\"Could not find any lapis ore nearby.\");\n  }\n}", "description": "async function mineLapisOre(bot) {\n    // The function focuses on mining a lapis ore block if one is found nearby. It first searches for a lapis ore block within a 32-block radius. If a lapis ore block is located, it checks for an iron pickaxe in the inventory and equips it if available. After equipping the iron pickaxe, the function proceeds to mine the lapis ore block. If no lapis ore block is found or if the iron pickaxe is not available, appropriate messages are generated.\n}", "created_by": "bot", "created_by_name": "bot", "created_at": "2025-12-28T17:51:28.423713", "version": 6, "success_count": 0, "fail_count": 0}, "mineThreeCopperOre": {"code": "async function mineThreeCopperOre(bot) {\n  const mcData = require('minecraft-data')(bot.version);\n\n  // Step 1: Locate the nearest copper ore blocks\n  let copperOreBlocks = bot.findBlocks({\n    matching: mcData.blocksByName[\"copper_ore\"].id,\n    maxDistance: 32,\n    count: 3\n  });\n\n  // Step 2: Equip the iron pickaxe if not already equipped\n  const ironPickaxe = bot.inventory.findInventoryItem(mcData.itemsByName[\"iron_pickaxe\"].id);\n  if (ironPickaxe) {\n    await bot.equip(ironPickaxe, \"hand\");\n    bot.chat(\"Equipped iron pickaxe.\");\n  } else {\n    bot.chat(\"No iron pickaxe available.\");\n    return;\n  }\n\n  // Step 3: Mine the copper ore blocks\n  if (copperOreBlocks.length >= 3) {\n    bot.chat(\"Found enough copper ore, mining now.\");\n    await mineBlock(bot, \"copper_ore\", 3);\n    bot.chat(\"Mined 3 copper ore.\");\n  } else {\n    bot.chat(\"Could not find enough copper ore.\");\n  }\n}", "description": "async function mineThreeCopperOre(bot) {\n    // The function focuses on mining three copper ore blocks using an iron pickaxe. It first locates the nearest copper ore blocks within a specified distance. If an iron pickaxe is available in the inventory, it equips the pickaxe. After equipping, it checks if there are at least three copper ore blocks found. If sufficient copper ore is located, it proceeds to mine the blocks; otherwise, it indicates that not enough copper ore was found.\n}", "created_by": "bot", "created_by_name": "bot", "created_at": "2025-12-28T17:48:24.886296", "version": 2, "success_count": 0, "fail_count": 0}, "mineThreeLapisOre": {"code": "async function mineThreeLapisOre(bot) {\n  const mcData = require('minecraft-data')(bot.version);\n\n  // Step 1: Locate the nearest lapis ore blocks\n  let lapisOreBlocks = bot.findBlocks({\n    matching: mcData.blocksByName[\"lapis_ore\"].id,\n    maxDistance: 32,\n    count: 3\n  });\n\n  // Step 2: Equip the iron pickaxe if not already equipped\n  const ironPickaxe = bot.inventory.findInventoryItem(mcData.itemsByName[\"iron_pickaxe\"].id);\n  if (ironPickaxe) {\n    await bot.equip(ironPickaxe, \"hand\");\n    bot.chat(\"Equipped iron pickaxe.\");\n  } else {\n    bot.chat(\"No iron pickaxe available.\");\n    return;\n  }\n\n  // Step 3: Mine the lapis ore blocks\n  if (lapisOreBlocks.length >= 3) {\n    bot.chat(\"Found enough lapis ore, mining now.\");\n    await mineBlock(bot, \"lapis_ore\", 3);\n    bot.chat(\"Mined 3 lapis ore.\");\n  } else {\n    bot.chat(\"Could not find enough lapis ore.\");\n  }\n}", "description": "async function mineThreeLapisOre(bot) {\n    // The function focuses on mining three lapis ore blocks. It first locates the nearest lapis ore blocks within a 32-block radius. If an iron pickaxe is available, it equips the pickaxe for mining. Once equipped, it checks if there are at least three lapis ore blocks found. If sufficient lapis ore is located, it proceeds to mine the blocks. If not enough lapis ore is found, it indicates the failure to locate the required amount.\n}", "created_by": "bot", "created_by_name": "bot", "created_at": "2025-12-28T17:52:13.588376", "version": 1, "success_count": 0, "fail_count": 0}, "mineOneLapisOre": {"code": "async function mineOneLapisOre(bot) {\n  const mcData = require('minecraft-data')(bot.version);\n\n  // Step 1: Locate the lapis ore block nearby\n  const lapisOreBlock = bot.findBlock({\n    matching: mcData.blocksByName[\"lapis_ore\"].id,\n    maxDistance: 32\n  });\n\n  // Step 2: Check if a lapis ore block was found\n  if (lapisOreBlock) {\n    bot.chat(\"Found lapis ore, preparing to mine it.\");\n\n    // Step 3: Equip the iron pickaxe if not already equipped\n    const ironPickaxe = bot.inventory.findInventoryItem(mcData.itemsByName[\"iron_pickaxe\"].id);\n    if (ironPickaxe) {\n      await bot.equip(ironPickaxe, \"hand\");\n      bot.chat(\"Equipped iron pickaxe.\");\n    } else {\n      bot.chat(\"No iron pickaxe available.\");\n      return;\n    }\n\n    // Step 4: Mine the lapis ore block\n    await mineBlock(bot, \"lapis_ore\", 1);\n    bot.chat(\"Mined 1 lapis ore.\");\n  } else {\n    bot.chat(\"Could not find any lapis ore nearby.\");\n  }\n}", "description": "async function mineOneLapisOre(bot) {\n    // The function focuses on mining a single lapis ore block. It begins by searching for a lapis ore block within a 32-block radius. If a lapis ore block is found, it checks for the presence of an iron pickaxe in the inventory. If the iron pickaxe is available, it equips it and proceeds to mine the lapis ore block. If no lapis ore is found or the iron pickaxe is unavailable, appropriate messages are displayed.\n}", "created_by": "bot", "created_by_name": "bot", "created_at": "2025-12-28T17:52:36.843536", "version": 1, "success_count": 0, "fail_count": 0}, "mineThreeIronOre": {"code": "async function mineThreeIronOre(bot) {\n  const mcData = require('minecraft-data')(bot.version);\n\n  // Step 1: Explore until iron ore is found\n  const ironOreBlock = await exploreUntil(bot, new Vec3(0, -1, 0), 60, () => {\n    return bot.findBlock({\n      matching: mcData.blocksByName[\"iron_ore\"].id,\n      maxDistance: 32\n    });\n  });\n\n  // Step 2: Check if iron ore was found\n  if (ironOreBlock) {\n    bot.chat(\"Found iron ore, preparing to mine.\");\n\n    // Step 3: Equip the iron pickaxe if not already equipped\n    const ironPickaxe = bot.inventory.findInventoryItem(mcData.itemsByName[\"iron_pickaxe\"].id);\n    if (ironPickaxe) {\n      await bot.equip(ironPickaxe, \"hand\");\n      bot.chat(\"Equipped iron pickaxe.\");\n    } else {\n      bot.chat(\"No iron pickaxe available.\");\n      return;\n    }\n\n    // Step 4: Mine the iron ore blocks\n    await mineBlock(bot, \"iron_ore\", 3);\n    bot.chat(\"Mined 3 iron ore.\");\n  } else {\n    bot.chat(\"Could not find any iron ore nearby.\");\n  }\n}", "description": "async function mineThreeIronOre(bot) {\n    // The function focuses on mining three iron ore blocks. It begins by exploring the surroundings until an iron ore block is located. If an iron ore block is found, it checks for the availability of an iron pickaxe in the inventory. If the iron pickaxe is available, it equips it and proceeds to mine three iron ore blocks. If no iron ore is found or the pickaxe is unavailable, appropriate messages are displayed.\n}", "created_by": "bot", "created_by_name": "bot", "created_at": "2025-12-28T17:59:51.172855", "version": 6, "success_count": 0, "fail_count": 0}, "mineGraniteBlock": {"code": "async function mineGraniteBlock(bot) {\n  const mcData = require('minecraft-data')(bot.version);\n\n  // Step 1: Locate the granite block nearby\n  const graniteBlock = bot.findBlock({\n    matching: mcData.blocksByName[\"granite\"].id,\n    maxDistance: 32\n  });\n\n  // Step 2: Check if a granite block was found\n  if (graniteBlock) {\n    bot.chat(\"Found granite block, preparing to mine it.\");\n\n    // Step 3: Mine the granite block\n    await mineBlock(bot, \"granite\", 1);\n    bot.chat(\"Mined 1 granite block.\");\n  } else {\n    bot.chat(\"Could not find any granite block nearby.\");\n  }\n}", "description": "async function mineGraniteBlock(bot) {\n    // The function focuses on mining a granite block if one is located nearby. It first searches for a granite block within a 32-block radius. If a granite block is found, it prepares to mine it and proceeds to mine one granite block. After successfully mining, it confirms the action. If no granite block is found, it indicates that none are nearby.\n}", "created_by": "bot", "created_by_name": "bot", "created_at": "2025-12-28T17:57:15.870891", "version": 1, "success_count": 0, "fail_count": 0}, "mineOneDioriteBlock": {"code": "async function mineOneDioriteBlock(bot) {\n  const mcData = require('minecraft-data')(bot.version);\n\n  // Step 1: Locate the nearest diorite block\n  const dioriteBlock = bot.findBlock({\n    matching: mcData.blocksByName[\"diorite\"].id,\n    maxDistance: 32\n  });\n\n  // Step 2: Check if a diorite block was found\n  if (dioriteBlock) {\n    bot.chat(\"Found diorite, preparing to mine it.\");\n\n    // Step 3: Equip the iron pickaxe if not already equipped\n    const ironPickaxe = bot.inventory.findInventoryItem(mcData.itemsByName[\"iron_pickaxe\"].id);\n    if (ironPickaxe) {\n      await bot.equip(ironPickaxe, \"hand\");\n      bot.chat(\"Equipped iron pickaxe.\");\n    } else {\n      bot.chat(\"No iron pickaxe available.\");\n      return;\n    }\n\n    // Step 4: Mine the diorite block\n    await mineBlock(bot, \"diorite\", 1);\n    bot.chat(\"Mined 1 diorite block.\");\n  } else {\n    bot.chat(\"Could not find any diorite nearby.\");\n  }\n}", "description": "async function mineOneDioriteBlock(bot) {\n    // The function focuses on mining a single diorite block. It begins by locating the nearest diorite block within a specified distance. If a diorite block is found, it checks for an iron pickaxe in the inventory and equips it if available. Once equipped, the function proceeds to mine the diorite block. If no diorite block is found or if the iron pickaxe is not available, appropriate messages are displayed.\n}", "created_by": "bot", "created_by_name": "bot", "created_at": "2025-12-28T17:59:05.098615", "version": 1, "success_count": 0, "fail_count": 0}, "smeltOneRawIron": {"code": "async function smeltOneRawIron(bot) {\n  const mcData = require('minecraft-data')(bot.version);\n\n  // Step 1: Check if a furnace is already placed nearby\n  let furnaceBlock = bot.findBlock({\n    matching: mcData.blocksByName.furnace.id,\n    maxDistance: 32\n  });\n\n  // If no furnace is found, place one from the inventory\n  if (!furnaceBlock) {\n    const furnacePosition = bot.entity.position.offset(1, 0, 0);\n    await placeItem(bot, \"furnace\", furnacePosition);\n    bot.chat(\"Placed a furnace.\");\n    furnaceBlock = bot.blockAt(furnacePosition);\n  }\n\n  // Step 2: Smelt 1 raw iron using coal as fuel\n  const rawIronCount = 1;\n  const fuelName = \"coal\";\n  await smeltItem(bot, \"raw_iron\", fuelName, rawIronCount);\n  bot.chat(\"Smelted 1 raw iron into an iron ingot.\");\n}", "description": "async function smeltOneRawIron(bot) {\n    // The function focuses on smelting one raw iron into an iron ingot. It first checks for the presence of a furnace within a 32-block radius. If no furnace is found, it places one from the inventory at a specified position. After ensuring a furnace is available, it proceeds to smelt one raw iron using coal as fuel. Finally, it confirms the successful smelting of the raw iron into an iron ingot.\n}", "created_by": "bot", "created_by_name": "bot", "created_at": "2025-12-28T18:01:35.273995", "version": 1, "success_count": 0, "fail_count": 0}, "obtainAcaciaLogs": {"code": "async function obtainAcaciaLogs(bot) {\n  const mcData = require('minecraft-data')(bot.version);\n\n  // Check how many acacia logs are needed\n  const logsNeeded = 4;\n  const currentLogs = bot.inventory.count(mcData.itemsByName[\"acacia_log\"].id);\n  const logsToMine = logsNeeded - currentLogs;\n  if (logsToMine > 0) {\n    bot.chat(`Need to mine ${logsToMine} more acacia logs.`);\n\n    // Locate the nearest acacia log blocks\n    const acaciaLogBlocks = bot.findBlocks({\n      matching: mcData.blocksByName[\"acacia_log\"].id,\n      maxDistance: 32,\n      count: logsToMine\n    });\n    if (acaciaLogBlocks.length > 0) {\n      bot.chat(\"Found acacia logs, mining now.\");\n      await mineBlock(bot, \"acacia_log\", logsToMine);\n      bot.chat(`Mined ${logsToMine} acacia logs.`);\n    } else {\n      bot.chat(\"Could not find enough acacia logs nearby.\");\n    }\n  } else {\n    bot.chat(\"Already have enough acacia logs.\");\n  }\n}", "description": "async function obtainAcaciaLogs(bot) {\n    // The function focuses on obtaining acacia logs for the bot's inventory. It first checks how many acacia logs are needed and calculates the difference from the current inventory. If more logs are required, it locates the nearest acacia log blocks within a specified distance. If sufficient acacia logs are found, the bot proceeds to mine the required amount. If not enough logs are available nearby, it notifies that there are insufficient logs. If the bot already has enough logs, it simply states that no further action is needed.\n}", "created_by": "scout", "created_by_name": "Scout", "created_at": "2025-12-28T18:12:44.227218", "version": 1, "success_count": 0, "fail_count": 0}, "mineTwentyCobblestone": {"code": "async function mineTwentyCobblestone(bot) {\n  const mcData = require('minecraft-data')(bot.version);\n\n  // Step 1: Equip the iron pickaxe\n  const ironPickaxe = bot.inventory.findInventoryItem(mcData.itemsByName[\"iron_pickaxe\"].id);\n  if (ironPickaxe) {\n    await bot.equip(ironPickaxe, \"hand\");\n    bot.chat(\"Equipped iron pickaxe.\");\n  } else {\n    bot.chat(\"No iron pickaxe available.\");\n    return;\n  }\n\n  // Step 2: Locate the nearest stone blocks\n  let stoneBlocks = bot.findBlocks({\n    matching: mcData.blocksByName[\"stone\"].id,\n    maxDistance: 32,\n    count: 20\n  });\n\n  // Step 3: If not enough stone blocks are found, explore until we find them\n  if (stoneBlocks.length < 20) {\n    bot.chat(\"Not enough stone nearby, exploring to find more.\");\n    stoneBlocks = await exploreUntil(bot, new Vec3(Math.random(), 0, Math.random()), 60, () => {\n      return bot.findBlocks({\n        matching: mcData.blocksByName[\"stone\"].id,\n        maxDistance: 32,\n        count: 20\n      });\n    });\n  }\n\n  // Step 4: Mine the stone blocks\n  if (stoneBlocks.length >= 20) {\n    bot.chat(\"Found enough stone, mining now.\");\n    await mineBlock(bot, \"stone\", 20);\n    bot.chat(\"Mined 20 cobblestone.\");\n  } else {\n    bot.chat(\"Could not find enough stone.\");\n  }\n}", "description": "async function mineTwentyCobblestone(bot) {\n    // The function focuses on mining 20 cobblestones using an iron pickaxe. It first checks if an iron pickaxe is available in the inventory and equips it if found. Next, it attempts to locate the nearest stone blocks within a specified distance. If fewer than 20 stone blocks are found, the function explores the area until enough stone blocks are located. Once sufficient stone blocks are available, it proceeds to mine 20 cobblestones. If not enough stone is found after exploration, it indicates the failure to find the required amount.\n}", "created_by": "scout", "created_by_name": "Scout", "created_at": "2025-12-28T18:13:51.532447", "version": 1, "success_count": 0, "fail_count": 0}, "mineOneWoodLog": {"code": "async function mineOneWoodLog(bot) {\n  const mcData = require('minecraft-data')(bot.version);\n\n  // Step 1: Locate the nearest acacia log block\n  const acaciaLogBlock = bot.findBlock({\n    matching: mcData.blocksByName[\"acacia_log\"].id,\n    maxDistance: 32\n  });\n\n  // Step 2: Check if an acacia log block was found\n  if (acaciaLogBlock) {\n    bot.chat(\"Found an acacia log, mining it now.\");\n    // Step 3: Mine the acacia log block\n    await mineBlock(bot, \"acacia_log\", 1);\n    bot.chat(\"Mined 1 acacia log.\");\n  } else {\n    bot.chat(\"Could not find any acacia logs nearby, exploring.\");\n    // Explore until an acacia log is found\n    await exploreUntil(bot, new Vec3(Math.random(), 0, Math.random()), 60, () => {\n      return bot.findBlock({\n        matching: mcData.blocksByName[\"acacia_log\"].id,\n        maxDistance: 32\n      });\n    });\n    // Try mining again after exploration\n    await mineOneWoodLog(bot);\n  }\n}", "description": "async function mineOneWoodLog(bot) {\n    // The function focuses on mining a single acacia log. It first attempts to locate the nearest acacia log block within a specified distance. If an acacia log is found, it proceeds to mine the block and confirms the action. If no acacia log is detected, the function explores the surroundings until one is found. After exploration, it attempts to mine the acacia log again. This process ensures that the bot successfully acquires at least one acacia log.\n}", "created_by": "scout", "created_by_name": "Scout", "created_at": "2025-12-28T18:21:29.693854", "version": 2, "success_count": 0, "fail_count": 0}, "craftWoodenPlanks": {"code": "async function craftWoodenPlanks(bot) {\n  const mcData = require('minecraft-data')(bot.version);\n\n  // Check if the bot has at least 1 acacia log\n  const acaciaLogCount = bot.inventory.count(mcData.itemsByName[\"acacia_log\"].id);\n  if (acaciaLogCount >= 1) {\n    bot.chat(\"Crafting 4 wooden planks from 1 acacia log.\");\n\n    // Craft 4 wooden planks from 1 acacia log\n    await craftItem(bot, \"acacia_planks\", 1);\n    bot.chat(\"Crafted 4 wooden planks.\");\n  } else {\n    bot.chat(\"Not enough acacia logs to craft wooden planks.\");\n  }\n}", "description": "async function craftWoodenPlanks(bot) {\n    // The function is designed to craft wooden planks from acacia logs. It first checks if the bot has at least one acacia log in its inventory. If there is enough acacia log, it proceeds to craft four wooden planks from one log. After crafting, it confirms the successful creation of the wooden planks. If there are not enough acacia logs, it indicates that crafting cannot be performed.\n}", "created_by": "scout", "created_by_name": "Scout", "created_at": "2025-12-28T18:22:34.724343", "version": 3, "success_count": 0, "fail_count": 0}, "craftCraftingTable": {"code": "async function craftCraftingTable(bot) {\n  const mcData = require('minecraft-data')(bot.version);\n\n  // Check if there are enough acacia planks to craft a crafting table\n  const acaciaPlanksCount = bot.inventory.count(mcData.itemsByName[\"acacia_planks\"].id);\n  if (acaciaPlanksCount >= 4) {\n    bot.chat(\"Crafting a crafting table from 4 acacia planks.\");\n\n    // Craft the crafting table\n    await craftItem(bot, \"crafting_table\", 1);\n    bot.chat(\"Crafted a crafting table.\");\n  } else {\n    bot.chat(\"Not enough acacia planks to craft a crafting table.\");\n  }\n}", "description": "async function craftCraftingTable(bot) {\n    // The function is designed to craft a crafting table using acacia planks. It first checks if there are at least four acacia planks in the inventory. If there are enough planks, it proceeds to craft the crafting table. After successfully crafting, it confirms the action. If there are not enough planks, it indicates that crafting cannot be completed.\n}", "created_by": "minerva", "created_by_name": "Minerva", "created_at": "2025-12-28T18:19:40.993873", "version": 1, "success_count": 0, "fail_count": 0}, "mineAcaciaLogs": {"code": "async function mineAcaciaLogs(bot) {\n  const mcData = require('minecraft-data')(bot.version);\n\n  // Step 1: Determine how many more acacia logs are needed\n  const logsNeeded = 4;\n  const currentLogs = bot.inventory.count(mcData.itemsByName[\"acacia_log\"].id);\n  const logsToMine = logsNeeded - currentLogs;\n  if (logsToMine > 0) {\n    bot.chat(`Need to mine ${logsToMine} more acacia logs.`);\n\n    // Step 2: Locate the nearest acacia log blocks\n    const acaciaLogBlocks = bot.findBlocks({\n      matching: mcData.blocksByName[\"acacia_log\"].id,\n      maxDistance: 32,\n      count: logsToMine\n    });\n    if (acaciaLogBlocks.length > 0) {\n      bot.chat(\"Found acacia logs, mining now.\");\n      // Step 3: Mine the required number of acacia logs\n      await mineBlock(bot, \"acacia_log\", logsToMine);\n      bot.chat(`Mined ${logsToMine} acacia logs.`);\n    } else {\n      bot.chat(\"Could not find enough acacia logs nearby.\");\n    }\n  } else {\n    bot.chat(\"Already have enough acacia logs.\");\n  }\n}", "description": "async function mineAcaciaLogs(bot) {\n    // The function focuses on mining acacia logs to meet a specific requirement. It first checks how many acacia logs are currently in the inventory and determines how many more are needed. If additional logs are required, it searches for nearby acacia log blocks within a specified distance. If sufficient acacia logs are found, it proceeds to mine the required amount. Finally, it provides feedback on the mining process, including whether the logs were successfully mined or if there were not enough logs nearby.\n}", "created_by": "scout", "created_by_name": "Scout", "created_at": "2025-12-28T18:22:09.167588", "version": 1, "success_count": 0, "fail_count": 0}}